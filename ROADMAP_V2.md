# جان‌کلام — نقشهٔ راهِ نسخهٔ ۲ (ایده‌ها و اولویت‌ها)

> این سند بعد از تکمیل MVP (مرحله‌های ۰ تا ۴: منبع → خبر → خوشه‌بندی → جان‌کلامِ فارسی)
> نوشته شده. هدف: قابلیت‌هایی که «تمایز» می‌سازند، مرتب‌شده بر پایهٔ ارزش و میزان کار.
> راهنمای تلاش: S = کوچک (روی زیرساخت فعلی)، M = متوسط، L = بزرگ / نیازمند زیرساخت تازه.

---

## ردهٔ ۱ — بیشترین تمایز، کمترین کار (اول این‌ها)

این سه مورد روی همان مدل چهارلایه و موتور رتبه‌بندیِ موجود سوار می‌شوند و تقریباً هیچ‌کس رقیب آن‌ها را خوب انجام نمی‌دهد.

### ۱. پیگیری سرنوشتِ ادعاها (Claim tracking) — تلاش: M
هر «ادعا» یا «ابهام» در یک خبر، در طول زمان دنبال شود: آیا بعداً تأیید شد، رد شد، یا هنوز نامشخص است؟
- چرا متمایز است: پاسخگویی خبری؛ درست از دلِ لایه‌های «واقعیت/ابهام» ما بیرون می‌آید.
- می‌سازد روی: جدول `statements` + خوشه‌بندی؛ نیاز به یک وضعیت (`status`) و لینک‌کردن ادعا در طول زمان.
- خروجی کاربر: نشان «✓ تأیید شد / ✗ رد شد / … هنوز نامشخص» کنار هر ادعا.

### ۲. «از دیروز چه تغییر کرد؟» (What changed) — تلاش: S–M
تفاوتِ (diff) جان‌کلامِ امروزِ یک خبر با نسخهٔ دیروزش؛ کاربرِ دنبال‌کننده فقط تغییرات را ببیند.
- چرا متمایز است: وقتِ کاربر را واقعاً نجات می‌دهد؛ خبر را به «روند» تبدیل می‌کند نه «عکسِ لحظه‌ای».
- می‌سازد روی: نگه‌داشتنِ نسخه‌های جان‌کلام (versioning ساده روی `stories`).

### ۳. شکافِ توجه: رسانه در برابر مردم — تلاش: M
مقایسهٔ «توجه رسانه‌ها» (از حجم/شتاب پوششِ خودمان) با «توجه مردم» (گوگل ترندز به‌عنوان سیگنالِ مکمل).
- چرا متمایز است: یک بینشِ تازه؛ «در رسانه‌ها داغ است ولی مردم بی‌اعتنا» یا برعکس.
- می‌سازد روی: بخش «بورس خبرها» + امتیاز اهمیت؛ افزودن یک منبع سیگنالِ بیرونی.

---

## ردهٔ ۲ — گسترشِ «آرشیو» و «بورس خبرها»

### آرشیو و بُعد زمانی
- **خط زمانی رویداد** (M): هر رویداد یک تایم‌لاین از خبرهای خوشه‌ای در طول زمان.
- **زمینهٔ تاریخی خودکار** (M): «این چندمین دور از این رویداد است»، با لینک به دفعات قبل.
- **جست‌وجوی آرشیوِ موضوع‌محور** (S): همهٔ خبرهای یک موضوع/شخص/مکان در گذر زمان.
- **کپسول هفتگی/ماهانه** (S): خلاصهٔ یک موضوع در یک بازه.

### بورس خبرها (فراتر از نسخهٔ فعلی)
- **روند در طول زمان** (S): نمودار توجهِ هر موضوع در چند هفته، نه فقط امروز.
- **شاخصِ اجماع/قطعیت** (S): منابع چقدر هم‌نظرند؛ عددی که در زمان تغییر می‌کند.
- **تشخیص جهش / بریکینگ** (M): وقتی شتابِ موضوعی ناگهان بالا رفت، اعلان.
- **لیدربوردِ منابع** (S): کدام منبع زودتر و دقیق‌تر پوشش داد.
- **سیگنال‌های نوظهور** (M): موضوعاتی که تازه دارند شکل می‌گیرند و هنوز ترند نشده‌اند.

---

## ردهٔ ۳ — سرمایه‌گذاری‌های بزرگ‌تر (بعد از جاافتادنِ ردهٔ ۱ و ۲)

- **بریفِ صبح/عصرِ شخصی** (M): خلاصهٔ روزانه بر پایهٔ موضوعاتِ دنبال‌شده.
- **خلاصهٔ صوتی / پادکستِ خودکار فارسی** (L): TTS فارسی روی جان‌کلام.
- **صفحهٔ موجودیت‌ها + گراف** (L): هر شخص/سازمان/مکان صفحهٔ خودش با تاریخچه و ارتباط‌ها.
- **تشخیص قاب‌بندی (framing)** (M): هر منبع یک خبر را چطور قاب می‌کند — بدون برچسب سیاسی، فقط تفاوت روایت.
- **حالت «سریع» در برابر «عمیق»** (S): هم خلاصهٔ ۳۰ثانیه‌ای، هم تحلیل کامل.
- **شاخصِ اعتبارِ شفافِ منابع** (M): چراییِ رتبهٔ هر منبع پیدا باشد.
- **اعلان/ویجت، سپس وب و iOS** (L): گسترشِ سطحِ عرضه.

---

## اصول ثابت (در همهٔ این‌ها رعایت شود)
- تفکیک «واقعیت / دیدگاه منبع / ترکیب / ابهام» هرگز شکسته نشود.
- ارتباط با ایران هیچ‌وقت ساختگی نباشد.
- خروجی هوش مصنوعی کش شود؛ روی هر بازدید دوباره تولید نشود.
- بدون برچسبِ سیاسی مگر با فراداده/شواهدِ معتبر.
- کلیدها و اسرار فقط سمت سرور.

## پیشنهادِ ترتیب اجرا
۱ (پیگیری ادعا) → ۲ (چه تغییر کرد) → ۳ (شکاف توجه) → سپس آرشیو (خط زمانی) و بورس خبرها (روند زمانی) → بعد سرمایه‌گذاری‌های بزرگ.
