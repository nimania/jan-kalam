# Core
fastapi>=0.141
uvicorn[standard]>=0.34
pydantic>=2.11
pydantic-settings>=2.7
SQLAlchemy>=2.0.30
alembic>=1.14

# Ingestion (Phase 2)
feedparser>=6.0
httpx>=0.28

# Postgres driver (production). Tests run on SQLite, so this is optional locally.
psycopg[binary]>=3.2 ; platform_system != "unknown"

# Dev / test
pytest>=8.3
