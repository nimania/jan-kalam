# استقرار رایگانِ جان‌کلام با GitHub (بدون سرور، سازگار با ایران)

این روش **صفر تومان** است و **نیازی به سرور ندارد**. کارش این است:
GitHub هر ۲ ساعت (روی سرورهای خودش، بیرون از ایران) خبرها را می‌گیرد، با هوش
مصنوعی جان‌کلام می‌سازد، و یک سایتِ آمادهٔ استاتیک منتشر می‌کند. تو فقط یک‌بار
راهش می‌اندازی.

---

## گام ۱ — حساب GitHub بساز
برو به **github.com** و یک حساب رایگان بساز.

## گام ۲ — پروژه را روی GitHub بگذار
ساده‌ترین راه برای غیربرنامه‌نویس: برنامهٔ **GitHub Desktop** را نصب کن
(desktop.github.com):
1. واردِ حسابت شو.
2. **File → Add Local Repository** و پوشهٔ `~/Documents/jan-kalam` را انتخاب کن.
   (اگر گفت اینجا مخزن نیست، **Create a repository** را بزن.)
3. یک پیام بنویس و **Commit** کن، بعد **Publish repository**.
   - توصیه: مخزن را **Public** بگذار (تا دقایقِ GitHub Actions نامحدود باشد).
     نگران نباش، کلید هوش مصنوعی به‌صورت «Secret» جدا نگه داشته می‌شود و در کد نیست.

## گام ۳ — کلید هوش مصنوعی را به‌عنوان Secret اضافه کن (برای جان‌کلامِ واقعی)
در صفحهٔ مخزن روی GitHub:
**Settings → Secrets and variables → Actions → New repository secret**
و این‌ها را یکی‌یکی اضافه کن:
- نام `AI_PROVIDER` مقدار `gemini`
- نام `AI_API_KEY` مقدار کلیدت
- نام `AI_MODEL_STRONG` مقدار `gemini-2.5-flash`

> اگر هنوز کلید نداری، این گام را رد کن؛ سایت با «حالت رایگانِ mock» ساخته می‌شود
> (متنِ جان‌کلام جای‌گیرنده است). هر وقت کلید داشتی، اضافه‌اش کن.
> کلید روی سرورِ GitHub (بیرون از ایران) استفاده می‌شود، پس مشکلِ دسترسیِ ایران را ندارد.

## گام ۴ — GitHub Pages را روشن کن
**Settings → Pages → Build and deployment → Source: GitHub Actions** را انتخاب کن.

## گام ۵ — اولین ساخت را اجرا کن
به تبِ **Actions** برو → ورک‌فلوی «Build & Deploy Jan Kalam» → **Run workflow**.
چند دقیقه بعد، پایینِ همان اجرا یا در **Settings → Pages**، آدرسِ سایتت را می‌بینی
(چیزی مثل `https://USERNAME.github.io/jan-kalam/`). بازش کن — جان‌کلام زنده است! 🎉

از این به بعد، هر ۲ ساعت خودکار به‌روز می‌شود.

---

## گام ۶ — وصل‌کردن دامنهٔ `pendar.io`
1. در مخزن: **Settings → Pages → Custom domain** → `pendar.io` (یا `news.pendar.io`)
   را وارد و **Save** کن. GitHub رکوردهای DNS لازم را نشانت می‌دهد.
2. در **SiteGround** (Site Tools → Domain → DNS Zone Editor) همان رکوردها را بساز:
   - برای خودِ `pendar.io`: چند رکورد **A** به آی‌پی‌های GitHub Pages
     (`185.199.108.153`، `185.199.109.153`، `185.199.110.153`، `185.199.111.153`).
   - برای ساب‌دامین (مثل `news`): یک رکورد **CNAME** به `USERNAME.github.io`.
3. کمی صبر کن تا DNS اعمال شود؛ بعد `https://pendar.io` بالا می‌آید (HTTPS خودکار است).

> **برای دسترسیِ بهتر از داخل ایران:** پیشنهاد می‌شود DNS دامنه را به **Cloudflare**
> (پلنِ رایگان) منتقل کنی و از آن‌جا به GitHub Pages وصل کنی؛ هم امن‌تر، هم در ایران در دسترس‌تر.

---

## نکته‌ها
- **هزینه:** صفر. (مخزنِ Public → دقایقِ Actions نامحدود. اگر Private باشد،
  هر ۲ ساعت هم داخلِ سقفِ رایگان ماهانه جا می‌شود.)
- **به‌روزرسانی محصول:** هر تغییری در کد را در GitHub Desktop **Commit & Push** کن؛
  سایت خودکار دوباره ساخته می‌شود.
- **محدودیتِ این نسخه:** «بپرس» پاسخ‌های از‌پیش‌آماده دارد (چون سرورِ زنده نیست).
  نسخهٔ کاملِ «بپرسِ آزاد» به همان استقرارِ سروری (Hetzner) نیاز دارد.
- تناوبِ به‌روزرسانی را می‌توانی در فایل `.github/workflows/build.yml` (خط `cron`) عوض کنی.
