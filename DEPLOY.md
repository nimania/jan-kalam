# راهنمای استقرار جان‌کلام (گام‌به‌گام)

این راهنما جان‌کلام را روی یک سرور راه می‌اندازد تا یک **آدرس اینترنتی واقعی**
بگیرد و ۲۴ساعته کار کند و **خودکار خبرها را بگیرد و پردازش کند**. لازم نیست
برنامه‌نویس باشی؛ فقط دستورها را کپی/پِیست کن.

با یک دستور، این چهار چیز بالا می‌آید: خودِ اپ + دیتابیس Postgres +
کارگرِ خودکارِ خبر + HTTPS خودکار.

---

## چیزهایی که لازم داری

1. **یک سرور (VPS)** — با اوبونتو (Ubuntu 24.04). ماهی حدود ۵ دلار کافی است.
   ارائه‌دهنده‌های معروف: Hetzner، DigitalOcean، Vultr، Contabo.
   **مهم:** سرور باید **بیرون از ایران** باشد (هم برای دسترسی به هوش مصنوعی،
   هم پایداری). یک سروری انتخاب کن که از داخل ایران هم **قابل‌دسترس** باشد
   (پیشنهاد: بعداً Cloudflare رایگان جلوش بگذار تا هم امن‌تر و هم در دسترس‌تر شود).
2. **(اختیاری ولی توصیه‌شده) یک دامنه** — مثل `news.example.com`. برای HTTPS خودکار.
   اگر فعلاً نداری، می‌توانی اول با IP سرور تست کنی.
3. **(اختیاری) کلید هوش مصنوعی** — بدون کلید هم کار می‌کند (حالت رایگانِ mock).
   برای جان‌کلامِ فارسیِ واقعی، یک کلید Gemini یا Anthropic روی سرور بگذار.

---

## گام‌ها

### ۱) به سرور وصل شو
از Terminal مک:
```
ssh root@SERVER_IP
```
`SERVER_IP` را با آی‌پیِ سرورت عوض کن. اولین بار «yes» را بزن و رمز را وارد کن.

### ۲) داکر را نصب کن (یک دستور)
```
curl -fsSL https://get.docker.com | sh
```

### ۳) پروژه را روی سرور بگذار
ساده‌ترین راه با Git (اگر پروژه را روی GitHub گذاشتی):
```
git clone https://github.com/USERNAME/jan-kalam.git
cd jan-kalam
```
یا از مکِ خودت با یک دستور کپی‌اش کن (این را روی **مک** بزن، نه سرور):
```
scp -r ~/Documents/jan-kalam root@SERVER_IP:/root/
```
بعد روی سرور: `cd /root/jan-kalam`

### ۴) تنظیمات را بساز
```
cp deploy.env.example .env
nano .env
```
در ویرایشگرِ `nano` این‌ها را پر کن، بعد `Ctrl+O` سپس `Enter` (ذخیره) و `Ctrl+X` (خروج):
- `POSTGRES_PASSWORD` — یک رمز طولانیِ تصادفی (حروف و عدد).
- `SITE_ADDRESS` — اگر دامنه داری: `news.example.com` ؛ اگر نه، فعلاً `:80` بگذار.
- برای هوش مصنوعیِ واقعی: `AI_PROVIDER=gemini` و `AI_API_KEY=...` و
  `AI_MODEL_STRONG=gemini-2.5-flash`. (اگر خالی بگذاری، حالت رایگانِ mock کار می‌کند.)

### ۵) بالا بیاور (یک دستور)
```
docker compose up -d --build
```
بارِ اول چند دقیقه طول می‌کشد (ساختِ image).

### ۶) منابع خبری اولیه را بارگذاری کن (فقط یک بار)
```
docker compose exec web python -m scripts.seed
```
این فهرست منابع (فارسی و جهانی) و موضوعات را وارد می‌کند. از همین لحظه،
کارگرِ خودکار شروع می‌کند به گرفتن و پردازش خبر (طبق `INGEST_POLL_INTERVAL_SECONDS`).

### ۷) بازش کن 🎉
- با دامنه: `https://news.example.com` (HTTPS خودکار فعال می‌شود)
- بدون دامنه: `http://SERVER_IP`

تمام. حالا یک محصولِ زندهٔ ۲۴ساعته داری.

---

## مدیریت روزمره

دیدنِ لاگ‌ها (اینکه چه می‌گذرد):
```
docker compose logs -f worker      # کارگرِ خبر
docker compose logs -f web         # خودِ اپ
```
اجرای دستیِ یک دورِ کامل (بدون صبر برای زمان‌بند):
```
docker compose exec worker python -c "from app.tasks.scheduler import run_full_cycle; print(run_full_cycle())"
```
به‌روزرسانی پس از تغییر کد:
```
git pull && docker compose up -d --build
```
خاموش‌کردن:
```
docker compose down
```
(داده‌ها در دیتابیس می‌مانند مگر اینکه `docker compose down -v` بزنی.)

---

## هزینه‌ها (تقریبی)
- سرور: ~۵ دلار در ماه
- دامنه: ~۱۰ دلار در سال (اختیاری)
- هوش مصنوعی: از «ماشین‌حساب اقتصاد جان‌کلام» — با مدل ارزان، ماهی چند دلار

## نکته‌های مهم
- کلید هوش مصنوعی فقط روی سرور می‌ماند و هیچ‌وقت به اپِ کاربر نمی‌رود.
- `.env` را هیچ‌وقت در Git نگذار (رمزها داخلش است).
- برای دسترسیِ بهتر از داخل ایران و امنیت بیشتر، Cloudflare (پلنِ رایگان) را
  جلوی دامنه‌ات قرار بده.
- انطباق قانونی/تحریمی پرداخت و میزبانی را پیش از راه‌اندازیِ تجاری بررسی کن.
